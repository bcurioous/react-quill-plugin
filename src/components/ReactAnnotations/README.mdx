---
name: ReactAnnotations
menu: Components
route: /components/ReactAnnotations
---

import ReactAnnotations from './';
import { Playground, Props } from 'docz';

# ReactAnnotations

This component renders an HTML `<button>` and passes along all props.

<Props of={ReactAnnotations} />

## Example Usage

<Playground>
	<Button>I'm a Button!</Button>
</Playground>
